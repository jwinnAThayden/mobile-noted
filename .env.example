# Environment Variables for Railway Deployment
# Copy these to your Railway project's environment variables

# Security Configuration
SECRET_KEY=your-super-secret-key-change-this-in-production
NOTED_USERNAME=admin
NOTED_PASSWORD_HASH=scrypt:32768:8:1$your-generated-hash

# Flask Configuration
FLASK_ENV=production
PORT=5000

# Optional: Rate Limiting Configuration
RATELIMIT_STORAGE_URL=memory://

# Session Configuration (optional)
SESSION_COOKIE_SECURE=true
SESSION_COOKIE_HTTPONLY=true
SESSION_COOKIE_SAMESITE=Lax

## Instructions:
## 1. Generate a secure SECRET_KEY: python -c "import secrets; print(secrets.token_hex(32))"
## 2. Generate password hash: python -c "from werkzeug.security import generate_password_hash; print(generate_password_hash('your-password'))"
## 3. Set these variables in Railway project settings
## 4. Change NOTED_USERNAME to your preferred username
## 5. Use a strong password for hash generation